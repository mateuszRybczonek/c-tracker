{{#if isAuthenticated}}
  {{#paper-toolbar as |toolbar|}}
    {{#sticky-container options=stickyOptions }}
      {{#toolbar.tools}}
        {{ui/link-button destination='posts.index' iconButton=true icon="list"}}
        {{ui/link-button destination='posts.new' iconButton=true icon="add"}}
      {{/toolbar.tools}}
    {{/sticky-container}}
  {{/paper-toolbar}}
{{/if}}
{{outlet}}
